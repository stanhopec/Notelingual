<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.nait.cstanhope2.notelingual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_notelingual"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_notelingual_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NoteListActivity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoteViewActivity" />
        <activity android:name=".UserPrefsActivity" />
    </application>

</manifest>